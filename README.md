# Demo repository https://github.com/pmndrs/react-spring/pull/1150

### Development

```bash
yarn start
```

### Production build

Run:

```bash
yarn build
```

Serve `buid` directory with a static server (e.g. `serve`, `http-server`)
